[package]
name = "find_neighbors"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tree_algorithm = { path = "../tree_algorithm" }
sphfunctions = { path = "../sphfunctions" }
